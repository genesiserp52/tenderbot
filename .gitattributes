# Force LF for files that run on the Linux VPS, regardless of host OS,
# so bash/systemd don't choke on CRLF.
*.sh        text eol=lf
*.service   text eol=lf
*.py        text eol=lf
*.yaml      text eol=lf
*.yml       text eol=lf
